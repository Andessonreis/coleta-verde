spring.application.name=coletaverde

# URL BANCO DE DADOS
spring.jpa.hibernate.ddl-auto=update

spring.datasource.url=jdbc:mysql://localhost:3306/mydatabase
spring.datasource.username=root
spring.datasource.password=root
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.show-sql=true


#CONFIGURA\u00c7OES GERAISn

# Configura��o do H2
# spring.datasource.url=jdbc:h2:mem:testdb
# spring.datasource.driverClassName=org.h2.Driver
# spring.datasource.username=sa
# spring.datasource.password=
# spring.sql.init.platform=h2
# spring.h2.console.enabled=true

# Configura��o do Hibernate para atualizar automaticamente o esquema
spring.jpa.hibernate.ddl-auto=update

# Adiar a inicializa��o do DataSource ate o Hibernate criar o esquema
spring.jpa.defer-datasource-initialization=true

# Certificar que os scripts SQL ser�o executados ap�s a cria��o do esquema
spring.sql.init.mode=always

# Mostrar as consultas SQL no log para depura��o
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# N�veis de log
logging.level.root=INFO
logging.level.com.example.app=DEBUG
logging.level.org.springframework.web=DEBUG
logging.level.org.springframework.security=DEBUG

# Configura��es de sess�o
spring.session.store-type=none

api.security.token.secret=secretKey
api.security.token.expiration-hours=1
api.security.token.timezone=-03:00